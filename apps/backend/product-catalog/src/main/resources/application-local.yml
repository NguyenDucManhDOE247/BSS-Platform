# B-02 fix — see customer-service/application-local.yml for the full explanation.
# Port map: api-gateway 8080, customer-service 8081, product-catalog 8082 (this service),
# order-management 8083, billing-service 8084.
server:
  port: 8082

logging:
  level:
    com.bss: DEBUG
