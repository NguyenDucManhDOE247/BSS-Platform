apiVersion: v1
kind: Service
metadata:
  name: product-catalog
  namespace: bss
  labels:
    app: product-catalog
    tier: backend # B-24
spec:
  type: ClusterIP
  selector:
    app: product-catalog
  ports:
    - { name: http, port: 80, targetPort: 8080 }
