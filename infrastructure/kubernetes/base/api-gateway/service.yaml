apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: bss
  labels: { app: api-gateway, tier: edge } # B-24
spec:
  type: ClusterIP
  selector: { app: api-gateway }
  ports:
    - { name: http, port: 80, targetPort: 8080 }
