apiVersion: v1
kind: Service
metadata:
  name: order-management
  namespace: bss
  labels: { app: order-management, tier: backend } # B-24
spec:
  type: ClusterIP
  selector: { app: order-management }
  ports:
    - { name: http, port: 80, targetPort: 8080 }
