apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Root base — every service that ships in BSS Platform. Overlays consume this.
resources:
  - namespace.yaml
  - customer-service
  - product-catalog
  - order-management
  - billing-service
  - api-gateway
  - web-portal
  - admin-console
  - ingress.yaml
