apiVersion: v1
kind: Service
metadata:
  name: web-portal
  namespace: bss
  labels: { app: web-portal, tier: frontend } # B-24
spec:
  type: ClusterIP
  selector: { app: web-portal }
  ports:
    - { name: http, port: 80, targetPort: 8080 }
