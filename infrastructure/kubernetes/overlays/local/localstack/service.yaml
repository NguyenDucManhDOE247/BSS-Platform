apiVersion: v1
kind: Service
metadata:
  name: localstack
  namespace: bss
  labels:
    app: localstack
    tier: data
spec:
  selector:
    app: localstack
  ports:
    - { name: edge, port: 4566, targetPort: 4566 }
